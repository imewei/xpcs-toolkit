# Documentation build requirements for ReadTheDocs

# Sphinx and core extensions
sphinx>=7.0.0
sphinx-rtd-theme>=2.0.0
furo>=2023.9.10

# Sphinx extensions
sphinx-copybutton>=0.5.0
sphinx-design>=0.5.0
sphinx-autodoc-typehints>=1.24.0
myst-parser>=2.0.0

# Optional extensions (install if available)
nbsphinx>=0.9.0

# Scientific computing documentation support
numpydoc>=1.6.0

# Markdown support
myst-parser>=2.0.0

# For building documentation without full package dependencies
# (ReadTheDocs will install the package separately)

# Note: The main package dependencies are installed via the package itself
# as specified in the .readthedocs.yaml configuration